# AI Engine & Training

This component contains the core model training scripts for the Soft Computing mid-term project.

## Setup

Ensure you have `uv` installed. From this directory:

```bash
# Sync dependencies
uv sync
```

## Running

To run the main training script:

```bash
uv run main.py
```