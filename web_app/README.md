# Web Application

This directory contains the user interface and web services for the Soft Computing mid-term project.

## Setup

Ensure you have `uv` installed. From this directory:

```bash
# Sync dependencies
uv sync
```

## Running

To start the web application:

```bash
uv run main.py
```